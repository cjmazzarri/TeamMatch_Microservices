server:
  port: 8090

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/cjmazzarri/TeamMatch_Microservices
          searchPaths: config-data
          default-label: "develop"
          username: ${GIT_USER}
          password: ${GIT_PASSWORD}
  security:
    user:
      name: teammatch
      password: tmroot